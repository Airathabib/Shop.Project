import styled from "styled-components";

export const ListCompnents = {
	StyledList: styled.div`
  & > p {
    font-size: 1.2rem;
    color: rgba(0, 0, 0, 0.9);
  }
  & > div {
    gap: 10px;
  }
`
}
